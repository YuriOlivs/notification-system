spring:
  application:
    name: herald-scheduler

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-plataform: org.hibernate.dialect.PostgreSQLDialect

  profiles:
    active: dev

server:
  port: 8082